require 'spec_helper'

describe ActionParamsPermitter do
  it 'has a version number' do
    expect(ActionParamsPermitter::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
